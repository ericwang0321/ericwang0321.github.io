# Eric Wang — AI & Capital Markets Research

Personal research portfolio for Eric (Yidong) Wang, built as a static Next.js site and deployed through GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Production export

```bash
npm run pages:build
```

The static site is written to `out/`. GitHub Actions deploys that directory whenever `main` is updated.
